import React from 'react';
import ReactDOM from 'react-dom/client';
import Car from './1st Example';

const container = document.getElementById('root');
const root = ReactDOM.createRoot(container);
root.render(<Car/>)